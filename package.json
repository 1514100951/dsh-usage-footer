{
  "name": "dsh-usage-footer-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "DSH Web 用量/费用栏插件（独立项目）：底部一行显示 token 用量、消费估算与账户余额，并带设置界面的开关",
  "scripts": {
    "check": "node --check packages/dsh-usage-status/lib/index.js && node --check packages/dsh-usage-status/lib/pricing.js && node --check packages/dsh-usage-status/lib/ledger.js && node --check packages/dsh-usage-status/lib/platform.js && node --check packages/dsh-client-ui-usage-footer/lib/client.js",
    "test": "node test/baseline.test.mjs && node test/pricing.test.mjs && node test/ledger.test.mjs && node test/platform.test.mjs"
  }
}
